#xndir1.py (Write a Python function that takes two sets as input and returns a new set containing elements that are present in both input sets.)
x = set('iluzia')
y = set('zium')
z = x | y
print(z)
